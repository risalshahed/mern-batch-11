const Header = (props) => {
  // console.log(props.props);

  return (
    <h1>
      Header of Warm up's Application&nbsp;
      <span>
        { props.props }
      </span>
    </h1>
  )
}

export default Header;